[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "nft"

[functions."daily-articles"]
  schedule = "0 * * * *"

[functions."cleanup-articles"]
  schedule = "0 3 * * 0"

# stripe-webhook precisa receber o body raw para verificar a assinatura HMAC
[functions."stripe-webhook"]
  included_files = []
