# Chave da API Anthropic (Claude). OPCIONAL.
# Se definida, a conclusao clinica e redigida pelo modelo claude-opus-4-8.
# Se ausente, o sistema usa um gerador deterministico baseado em regras.
ANTHROPIC_API_KEY=

# Origens permitidas para CORS (separadas por virgula).
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
